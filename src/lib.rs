use std::collections::LinkedList;
use objects;

fn load_days_reminders()->LinkedList<entrys>{
	Date = DateTime::<Utc>::from(SystemTime::now()).date()

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
